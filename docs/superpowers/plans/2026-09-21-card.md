# Card — plan

Spec: `../specs/2026-09-21-card-design.md`.

1. `src/components/Card/Card.test.tsx` — the parts, `as`, the link, `ratio`, the stylesheet's holds, axe. Red.
2. `Card.tsx`, `Card.module.css`, `index.ts`; export from `src/index.ts`. Green.
3. Contrast cases in `src/tokens/contrast.test.ts`.
4. `app/card/page.tsx` with the Locations card rebuilt on a raised panel; nav entry (sixteen components); section card.
5. Browser: both modes, 320, keyboard focus, the overflow button over the link.
6. `npm run check`, `npm run build:docs`.
7. Code review, fixes, records (CHANGELOG, README counts, MEMORY.md).
