# Skeleton — plan

Spec: `../specs/2026-09-21-skeleton-design.md`.

1. `Skeleton` tests, then the component, stylesheet and index; export.
2. `app/skeleton/page.tsx`: the three shapes, a card and a table row composed, the `aria-busy` pattern; nav entry (twenty-three); section card.
3. Browser: the sweep in light and dark, on the page and on a card; text standing in its line; reduced motion.
4. `npm run check`, `npm run build:docs`; code review; records.
