# EmptyState — plan

Spec: `../specs/2026-09-22-empty-state-design.md`.

1. `EmptyState` tests, then the component, stylesheet and index; export.
2. Contrast cases.
3. `app/empty-state/page.tsx`: first use, no results in a Table, all done, a section, the dashed frame, an area that did not load, the drawn "+ Add" with the Button, the rules of content; nav entry (twenty-five); section card.
4. Browser: both sizes, in a Table, dashed, dark.
5. `npm run check`, `npm run build:docs`; code review; records.
