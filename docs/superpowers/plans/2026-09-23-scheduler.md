# Scheduler — plan

Spec: `../specs/2026-09-23-scheduler-design.md`. Phase 1 of two.

1. The categorical palette: twenty-four `category/*` theme tokens,
   `npm run build:css`, the contrast cases, the Colour page's fifth table,
   the README's counts, the Figma export.
2. `time.ts` and `layout.ts` with their tests; then the `Scheduler` tests,
   component, stylesheet, index, exports.
3. Browser: the week, the day with five people, a phone width with one
   column, the now line, selection and the keyboard, light and dark.
4. `app/scheduler/page.tsx` composing the drawn screen; nav entry
   (thirty-two); section card.
5. `npm run check`, `npm run build:docs`; code review; records.
