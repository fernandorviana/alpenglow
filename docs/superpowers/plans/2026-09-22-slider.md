# Slider — plan

Spec: `../specs/2026-09-22-slider-design.md`.

1. `Slider` tests, component, stylesheet, index, exports; contrast cases.
2. Browser: every drawn usage on the docs page, the range dragged from both
   ends, a typed value out of range, ticks, light and dark, a phone width.
3. `app/slider/page.tsx`; nav entry (thirty); section card.
4. `npm run check`, `npm run build:docs`; code review; records.
