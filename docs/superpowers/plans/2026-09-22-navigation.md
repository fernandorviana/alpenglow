# SideNav, SideNavSecondary and TopBar — plan

Spec: `../specs/2026-09-22-navigation-design.md`.

1. `SideNav` tests, component, stylesheet, index; `SideNavSecondary` the
   same; `TopBar` the same; exports; contrast cases.
2. Browser: the composed shell on the docs page, collapsed and open, the
   secondary folded and collapsed, the narrow dialog forced through
   `narrow`, light and dark, a phone width.
3. `app/navigation/page.tsx`: the shell with controls; nav entry
   (twenty-eight); section card.
4. `npm run check`, `npm run build:docs`; code review; records.
