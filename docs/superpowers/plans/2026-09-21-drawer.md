# Drawer — plan

Spec: `../specs/2026-09-21-drawer-design.md`.

1. `Drawer` tests, then the component, stylesheet and index; export.
2. Contrast cases: words on both surfaces, the handle's two lines.
3. `app/drawer/page.tsx`: New appointment over the content with the "leave without saving?" Dialog; a day's list with the panel beside it, resizable and expandable. Nav entry (twenty-one); section card.
4. Browser: the top layer, focus in and back (with the Dialog), Esc with a Select open, drag and clamp, expand and collapse, inline making room, a phone.
5. `npm run check`, `npm run build:docs`; code review; records; the roadmap's line revised.
