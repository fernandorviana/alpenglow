# Progress — plan

Spec: `../specs/2026-09-22-progress-design.md`.

1. `Progress` tests, then the component, stylesheet and index; export from
   `src/index.ts`; contrast cases.
2. Browser: does the element's own background paint under `appearance: none`
   in Chromium with the pseudo-elements transparent; does `background-size`
   transition; the indeterminate band; light and dark; reduced motion.
3. `app/progress/page.tsx`: try it with a value control, label and value,
   sizes, tones for done and failed, indeterminate, the upload card, the
   onboarding top bar with the composed gradient and its reason, which one
   (Loader, Skeleton, Progress), props; nav entry (twenty-six); section card.
4. `npm run check`, `npm run build:docs`; code review; records: CHANGELOG,
   README count, MEMORY.md claim.
