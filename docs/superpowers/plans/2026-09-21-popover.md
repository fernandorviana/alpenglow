# Popover — plan

Spec: `../specs/2026-09-21-popover-design.md`.

1. `floating.module.css`; DropdownMenu, DatePicker and the Pagination's page size moved onto it; their stylesheet assertions moved with the rules. Suite green, then the menu checked in the browser.
2. `Popover` tests, then the component, stylesheet and index; export.
3. Contrast cases.
4. `app/popover/page.tsx`; nav entry (eighteen); section card.
5. Browser: both modes, a DatePicker inside it, 320.
6. `npm run check`, `npm run build:docs`; code review; records.
7. Next, its own cycle: `Select` as a listbox on this surface, `NativeSelect` for today's.
