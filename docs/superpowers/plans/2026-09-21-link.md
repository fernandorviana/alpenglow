# Link — plan

Spec: `../specs/2026-09-21-link-design.md`.

1. `src/components/visuallyHidden.module.css`; Toast, Alert, Pagination moved onto it. Done first, suite green.
2. `src/components/linkRender.ts` — the shared `render` types.
3. `Link` tests, then `Link.tsx`, `Link.module.css`, `index.ts`, export.
4. `Button` tests for `href`, then the component and the stylesheet's selectors.
5. Contrast cases.
6. `app/link/page.tsx`, a section on `/button`, nav entry (seventeen), section card, `.prose a`.
7. Browser: both modes, keyboard, 320.
8. `npm run check`, `npm run build:docs`; code review; records.
