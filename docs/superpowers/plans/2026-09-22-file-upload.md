# FileUpload — plan

Spec: `../specs/2026-09-22-file-upload-design.md`.

1. `FileUpload` tests, component, stylesheet, index, exports; contrast cases.
2. Browser: the zone at rest, dragged over (a synthetic dragover), the
   simulated upload, a rejection, compact and tile, light and dark, a phone
   width.
3. `app/file-upload/page.tsx`; nav entry (thirty-one); section card.
4. `npm run check`, `npm run build:docs`; code review; records.
