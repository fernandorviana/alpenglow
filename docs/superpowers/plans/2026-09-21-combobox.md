# Combobox — plan

Spec: `../specs/2026-09-21-combobox-design.md`.

1. `src/components/listbox/`: move `options.ts`, add `fold`, `contains`, `filterEntries`; write `OptionList`; move the Select onto it. Suite green.
2. `Combobox` tests, then the component, stylesheet and index; export.
3. Contrast cases.
4. `app/combobox/page.tsx`; nav entry (nineteen); section card.
5. Browser: both forms, both modes, the pinned buttons.
6. `npm run check`, `npm run build:docs`; code review; records.
