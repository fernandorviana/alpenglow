# Select — plan

Spec: `../specs/2026-09-21-select-design.md`.

1. Rename today's component to `NativeSelect` (folder, files, exports, the Field's test). Suite green.
2. `--floating-overflow` on the floating surface, so a list can scroll without setting a property the surface sets.
3. `options.ts` and its tests; `Select` tests; the component, stylesheet and index; export.
4. Contrast cases.
5. `/select` rewritten with a section for `NativeSelect`; the Dialog, Popover and Components pages moved onto the new one.
6. Browser: both modes, inside a Popover, the keyboard.
7. `npm run check`, `npm run build:docs`; code review; records.
